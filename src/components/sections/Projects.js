import React from "react";
import styled from "styled-components";

const StyledSection = styled.section`
  height: 100vh;
  padding: 100px 0px;
  margin: 0px auto;
`;

const Projects = () => {
  return <StyledSection id="projects">I'm a Projects</StyledSection>;
};

export default Projects;
