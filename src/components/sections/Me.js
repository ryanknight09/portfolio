import React from "react";
import styled from "styled-components";

const StyledSection = styled.section`
  height: 100vh;
  background: papayawhip;
  padding: 100px 0px;
  margin: 0px auto;
`;

const Me = () => {
  return <StyledSection>I'm a Me</StyledSection>;
};

export default Me;
